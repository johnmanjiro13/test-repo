name: auto-assign
on:
  pull_request:

jobs:
  auto-assign:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: johnmanjiro13/auto-assign@ad8360661a98996434386086951fdfad6ab24519
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
