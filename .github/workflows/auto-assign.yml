name: auto-assign
on:
  pull_request:

jobs:
  auto-assign:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: johnmanjiro13/auto-assign@0078b6dc122413239342756176e9423542be5c0a
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
